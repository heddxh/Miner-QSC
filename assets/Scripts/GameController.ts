import { _decorator } from 'cc';
import { SingletonController } from './SingletonComponent';
const { ccclass, property } = _decorator;

@ccclass('GameController')
export class GameController extends SingletonController {

    // 剩余时间
    
    // 响应UI事件
    // 生成矿物

    start() {

    }

    update(deltaTime: number) {
        
    }
}

